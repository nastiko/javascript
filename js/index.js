/*$(window).on('resize', function() {
    let windowScreen = $(this);
    if (windowScreen.width() <= 970) {
        $('.desktop-device, .desktop-img, .grid-container').hide();
        $('.mobile-device, .mobile-img, .mobile-container').show();
    } else {
        $('.desktop-device, .desktop-img, .grid-container').show();
        $('.mobile-device, .mobile-img, .mobile-container').hide();
    }
});*/
